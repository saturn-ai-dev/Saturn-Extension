import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saturn.saturnAndroidTest1',
  appName: 'saturn-android-raw-test',
  webDir: 'dist'
};

export default config;
